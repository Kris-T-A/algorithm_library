# Audio Transformer
Audio Transformer is a library that performs transformation on an audio file. This is done using the algorithms in the [Algorithm Library](../..). 

The main class is `AudioTransformer` that is given a const reference to an audio file during construction.